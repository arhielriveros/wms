{
  "name": "wms-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "web:dev": "npm --workspace @wms/supervisor-web run dev",
    "web:build": "npm --workspace @wms/supervisor-web run build",
    "web:typecheck": "npm --workspace @wms/supervisor-web run typecheck",
    "backend:build": "dotnet build src/backend/Wms.Backend.slnx",
    "backend:test": "dotnet test src/backend/Wms.Backend.slnx --no-build",
    "validate:docs": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-docs.ps1 && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-traceability.ps1",
    "validate": "npm run web:typecheck && npm run backend:build && npm run backend:test && npm run validate:docs"
  }
}
